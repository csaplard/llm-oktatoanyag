{
  "name": "katedra-llm-guide",
  "private": true,
  "version": "1.1.3",
  "description": "Offline-capable bilingual LLM learning guide",
  "scripts": {
    "build": "python3 scripts/build.py",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
